# Kopieer naar app/.env.local voor lokaal ontwikkelen. Op GitHub staan dezelfde twee als
# repository variables (Settings > Secrets and variables > Actions > Variables).
# De anon key is bedoeld voor gebruik in de browser; de rechten zitten in de database (RLS).
VITE_SUPABASE_URL=https://fhgghcksvnyxfwkielzx.supabase.co
VITE_SUPABASE_ANON_KEY=
